import { HoverColorsDirective } from './hover-colors.directive';

describe('HoverColorsDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverColorsDirective();
    expect(directive).toBeTruthy();
  });
});
